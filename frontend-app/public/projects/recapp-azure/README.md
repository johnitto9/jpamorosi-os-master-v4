# Media — RecApp / Azure PoC (`recapp-azure`)

Optional future home for dedicated assets. This project currently renders its
card/room hero from an existing image referenced in `content/projects.ts`.
Until dedicated assets exist here, SmartImage falls back gracefully — do **not**
commit fake images.

Note: the project slug is `recapp-azure` (folder must match the slug used in
`content/projects.ts` for paths to resolve).

Expected files (see `docs/PROJECT_MEDIA_GUIDE.md`):

- `logo.svg`
- `hero.webp` (16:10)
- `bg.webp`
- `screenshots/*.webp` (16:9)
